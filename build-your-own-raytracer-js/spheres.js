class Sphere {
    constructor(center, radius, color, material) {
        this.centre = center;       // Keep 'centre' for consistency with existing code
        this.r = radius;           // Keep 'r' for consistency with existing code
        this.color = color;
        this.material = material;
    }
}